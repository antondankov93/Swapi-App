import React from "react";
import s from "./chartsPage.module.scss";
import {NavLink} from "react-router-dom";
import cn from "classnames";

export const ChartsPage = () => {
    return (
        <section className={s.mainWrapper}>
            <NavLink className={cn(s.blockWrapper,s.characters)} to='/'>
                <div className={s.blockHeader}>My favorites</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.planets)} to='/'>
                <div className={s.blockHeader}>Chart 2</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.starships)} to='/'>
                <div className={s.blockHeader}>Chart 3</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.films)} to='/'>
                <div className={s.blockHeader}>Chart 4</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.starships)} to='/'>
                <div className={s.blockHeader}>Chart 5</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.films)} to='/'>
                <div className={s.blockHeader}>Chart 6</div>
            </NavLink>
        </section>
    )
}