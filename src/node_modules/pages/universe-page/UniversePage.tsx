 import React from "react";
import {NavLink} from "react-router-dom";
import s from './universePage.module.scss';
import cn from 'classnames'

export const UniversePage = () => {
    return (
        <section className={s.mainWrapper}>
            <NavLink className={cn(s.blockWrapper,s.characters)} to='/catalog/characters'>
                <div className={s.blockHeader}>Characters</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.planets)} to='/catalog/planets'>
                <div className={s.blockHeader}>Planets</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.starships)} to='/catalog/starships'>
                <div className={s.blockHeader}>Starships</div>
            </NavLink>

            <NavLink className={cn(s.blockWrapper,s.films)} to='/catalog/films'>
                <div className={s.blockHeader}>Films</div>
            </NavLink>
        </section>
    )
}