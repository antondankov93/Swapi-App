import React from "react";
import s from './header.module.scss'
import {NavLink} from "react-router-dom";
import cn from 'classnames';


export const Header = () => {
    return (
        <header className={s.mainWrapper}>
            <nav className={s.navWrapper}>
                    <div className={s.logoLink}>
                        <NavLink to='/'>
                            <img src={require("assets/main-logo.png")} alt=""/>
                        </NavLink>
                    </div>
                    <div className={cn(s.navLinks, s.navLinksMain)}>
                        <NavLink to='/'>Main Page</NavLink>
                    </div>
                    <div className={cn(s.navLinks, s.navLinksCatalog)}>
                        <NavLink to='/catalog'>Catalog</NavLink>
                    </div>
                    <div className={cn(s.navLinks, s.navLinksGraphs)}>
                        <NavLink to='/graphs'>Graphs</NavLink>
                    </div>
                    <div className={cn(s.navLinks, s.accountLinks)}>
                        <NavLink to='/account'>My account</NavLink>
                    </div>
            </nav>
        </header>
    )
}