import React from "react";
import s from './error.module.scss'

export const Error = () => {
    return (
        <div className={s.mainWrapper}>
            <p className={s.errorMessage}>
                <span className={s.errorSpan}>Error!</span><br/>
                 it must be a partisans of empire was broke our systems!<br/>
            don't worry. our droids have already left to fix it.</p>
        </div>
    )
}