import React from "react";
import s from './planets.module.scss'
import {Carousel} from "react-bootstrap";
import cn from "classnames";
import arrData from "components/mockesData";

export const Planets = () => {

    return (
        <div className={s.mainWrapper}>
            <Carousel>
                {arrData.map(d =>
                    <Carousel.Item>
                        <div className={s.mainContent}>
                            <div className={cn(s.characterInfo, s.left)}>
                                <p><span>name:</span> {d.name}</p>
                                <p><span>gender:</span> {d.gender}</p>
                                <p><span>birth year:</span> {d.birth_year}</p>
                                <p><span>height year:</span> {d.height}</p>
                                <p><span>mass:</span> {d.mass}</p>
                                <p><span>eye color:</span> {d.eye_color}</p>
                            </div>
                            <img
                                className={s.image}
                                src={`https://starwars-visualguide.com/assets/img/planets/${arrData.indexOf(d) + 1}.jpg`}
                            />
                            <div className={cn(s.characterInfo, s.right)}>
                                <p><span>home planet:</span> {d.name}</p>
                                <p><span>gender:</span> {d.gender}</p>
                                <p><span>birth year:</span> {d.birth_year}</p>
                                <p><span>height year:</span> {d.height}</p>
                                <p><span>mass:</span> {d.mass}</p>
                                <p><span>eye color:</span> {d.eye_color}</p></div>
                        </div>
                        <Carousel.Caption>
                            <h3>{d.name}</h3>
                            <p>{d.gender}</p>
                        </Carousel.Caption>
                    </Carousel.Item>
                )}


            </Carousel>
        </div>
    )
}