import {takeEvery, all ,call} from 'redux-saga/effects'



function* getAllCharactersInList() {
    yield takeEvery()
}

export function* charactersSaga() {
    yield all([call(getAllCharactersInList)]);
}