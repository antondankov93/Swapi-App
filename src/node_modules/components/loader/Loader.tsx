import React from "react";
import s from './loader.module.scss'
import {Spinner} from "react-bootstrap";

export const Loader = () => {

    return (
        <div className={s.mainWrapper}>
            <video
                preload="auto"
                autoplay="true"
                // @ts-ignore
                loop="true"
                // @ts-ignore
                muted="muted"
                height='600px'
            >
                <source src={require('../../assets/backgrounds/loader.mp4')}/>
            </video>
        </div>
    )
}