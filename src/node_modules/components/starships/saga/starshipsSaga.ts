import {takeEvery, all ,call} from 'redux-saga/effects'



function* getAllStarshipsInList() {
    yield takeEvery()
}

export function* planetsSaga() {
    yield all([call(getAllStarshipsInList)]);
}